package com.linkedin;

import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.support.PageFactory;
import org.testng.annotations.Test;

public class VerificationTextTest {
    //protected WebDriver driver;

    //public VerificationTextTest(WebDriver driver){
        //this.driver = driver;
    //}
    private LoginRegistrationPage loginRegistrationPage;
    @Test
   public void securityVerification(){
        //loginRegistrationPage = PageFactory.initElements(new FirefoxDriver(), LoginRegistrationPage.class);
        //loginRegistrationPage.open();
        //String email = "iuliia.shpilka@gmail.com";
        //loginRegistrationPage.submitRegistrationForm("First", "Last", email, "P@ssword123");
        //String text = driver.findElement(By.xpath("//div[contains(@class, 'alerts-cont') and //text()='Someone is already using that email.")).getText();
        //if (text=="Someone is already using that email."){
            //System.out.println("error message - Someone is already using that email.");
        }
       /// else {
            //System.out.println("error message isnt Someone is already using that email.");
        }
        //assert securityVerification form displayed;
        //WebElement errorMessage = driver.findElement(By.xpath("//div[contains(@class, 'alerts-cont')  and //text()='Someone�s already using that email.'"));
        //Assert.assertTrue(errorMessage.isDisplayed(),"Someone�s already using that email.");
       // loginRegistrationPage.close();
    //}
//}
